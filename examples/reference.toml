# =============================================================================
# GRAFT - reference.toml
# Annotated schema reference for Graft TOML.
# Some fields are parse-only today and do not affect Quadlet output yet.
# =============================================================================


# -----------------------------------------------------------------------------
# MVP - minimum working subset
# -----------------------------------------------------------------------------

# version =
# name =

# [parents]
# add =

# [deploy]
# enable =
# target =

# [config.runtime]
# mode =
# packages =
# command =

# [config.container]
# name =
# hostname =
# entrypoint =
# user =
# workingDir =
# stopSignal =
# stopTimeout =

# [config.container.environment]
# KEY =

# [[config.filesystem.volumes]]
# source =
# target =
# mode =

# [config.network]
# mode =
# publish =

# [config.security]
# dropCapabilities =
# noNewPrivileges =

# [config.service]
# type =
# restart =

# NixOS (explicit opt-in; Graft does NOT start Podman automatically)
# virtualisation.podman.enable =
# virtualisation.podman.dockerSocket.enable =
# virtualisation.podman.networkSocket.enable =


# =============================================================================
# Roadmap
# =============================================================================


# -----------------------------------------------------------------------------
# Graft schema - remaining options
# -----------------------------------------------------------------------------

# [parents]
# remove =                       # ["base/name"]
# set =                          # ["base/name"]

# [children]
# add =                          # ["child/name"]
# remove =                       # ["child/name"]
# set =                          # ["child/name"]

# [validation]
# level =                        # off | warn | strict

# [config.runtime.packageOps]
# add =                          # ["pkg"]
# remove =                       # ["pkg"]
# [[config.runtime.packageOps.replace]]
# name =                         # string
# with =                         # string

# [config.container] - advanced
# pod =                          # string
# group =                        # string | int
# timezone =                     # local | UTC | Europe/Amsterdam | ...
# notify =                       # container | healthy
# runInit =                      # true | false
# podmanArgs =                   # ["--arg"]
# globalArgs =                   # ["--arg"]
# ip =                           # string (IPv4)
# ip6 =                          # string (IPv6)
# networkAlias =                 # ["alias"]
# exposeHostPort =               # ["8080"]
# uidMap =                       # ["0:100000:65536"]
# gidMap =                       # ["0:100000:65536"]
# subUidMap =                    # string (e.g. "@user")
# subGidMap =                    # string
# shmSize =                      # string (e.g. "64m")
# mask =                         # ["/path"]
# unmaskPaths =                  # ["/path"]
# sysctl =                       # ["net.ipv4.ip_unprivileged_port_start=80"]
# logDriver =                    # journald | k8s-file | ...

# [config.container.annotations]
# key =                          # string

# [config.container.environmentFile]
#                                # ["/path/to/file"]

# [config.container.health]
# cmd =                          # string
# interval =                     # string (e.g. "30s")
# timeout =                      # string (e.g. "10s")
# retries =                      # int
# startPeriod =                  # string (e.g. "5s")
# onFailure =                    # kill | restart | stop | none
# startupCmd =                   # string
# startupInterval =              # string
# startupRetries =               # int
# startupSuccess =               # int
# startupTimeout =               # string

# [config.filesystem]
# readOnly =                     # true | false
# readOnlyTmpfs =                # true | false
# tmpfs =                        # ["/tmp"]
# mounts =                       # ["type=bind,src=/host,dst=/container"]

# [[config.filesystem.devices]]
# source =                       # string
# target =                       # string
# permissions =                  # string (e.g. "rwm")

# [config.network] - advanced
# dns =                          # ["1.1.1.1"]
# dnsOption =                    # ["string"]
# dnsSearch =                    # ["example.com"]
# addHost =                      # ["hostname:ip"]

# [[config.networks]]
# name =                         # string
# driver =                       # string
# internal =                     # true | false
# ipv6 =                         # true | false
# subnet =                       # string
# gateway =                      # string
# ipRange =                      # string
# dns =                          # ["1.1.1.1"]
# options =                      # string

# [[config.volumes]]
# name =                         # string
# driver =                       # string
# copy =                         # true | false
# options =                      # string

# [config.security] - advanced
# addCapabilities =              # ["NET_BIND_SERVICE"]
# privileged =                   # true | false
# seccompProfile =               # string (path)
# securityLabelDisable =         # true | false
# securityLabelFileType =        # string
# securityLabelLevel =           # string
# securityLabelNested =          # true | false
# securityLabelType =            # string
# securityOpt =                  # ["no-new-privileges"]
# userns =                       # keep-id | auto | ...

# [config.resources]
# memory =                       # string (e.g. "512m")
# memorySwap =                   # string
# cpus =                         # string (e.g. "0.5")
# cpuQuota =                     # string
# pidsLimit =                    # int
# ulimits =                      # ["nofile=1024:1024"]

# [[config.secrets]]
# name =                         # string
# target =                       # string
# type =                         # mount | env
# uid =                          # int
# gid =                          # int
# mode =                         # string (e.g. "0400")
# options =                      # string

# [config.session]
# stopOnExit =                   # true | false

# [config.workspace]
# mode =                         # none | copy
# source =                       # string (host path, default: ".")
# target =                       # string (container path, default: "/workspace")
# review =                       # diff
# promote =                      # off | prompt | auto
# excludePatterns =              # [".git", "node_modules"]

# [config.home]
# mode =                         # ephemeral | persistent | session
# source =                       # string (host path, supports "~")
# target =                       # string (default: "/home/user")
# review =                       # diff
# promote =                      # auto | prompt | never

# [[config.home.shadow]]
# container =                    # string (path inside container)
# host =                         # string (host path, supports "~")

# [config.attach]
# tmuxSession =                  # string (default: "main")
# shell =                        # string (default: "sh")
# startDelay =                   # string (e.g. "500ms", "2s")

# [config.service] - advanced
# restartSec =                   # string (e.g. "10s")
# timeoutStartSec =              # string (e.g. "2m")
# timeoutStopSec =               # string (e.g. "30s")
# remainAfterExit =              # true | false
# restartIfChanged =             # true | false

# [config.quadlet.container]    # raw passthrough (cannot set Rootfs, ContainerName, Exec, Network, Volume)
# [config.quadlet.service]      # raw passthrough
# [config.quadlet.install]      # raw passthrough


# ---------------------------------------------------------------------------
# Quadlet [Container] options (podman-systemd.unit(5))
# ---------------------------------------------------------------------------
# AddCapability =                # CAP_NET_ADMIN ...
# AddDevice =                    # /dev/fuse
# AddHost =                      # hostname:ip
# Annotation =                   # key=value
# AppArmor =                     # profile
# AutoUpdate =                   # registry | local
# CgroupsMode =                  # enabled | disabled | no-conmon | split
# ContainerName =                # string
# ContainersConfModule =         # /path/to/module.conf
# DNS =                          # 1.1.1.1
# DNSOption =                    # string
# DNSSearch =                    # example.com
# DropCapability =               # CAP_NET_RAW | all
# Entrypoint =                   # /bin/sh
# Environment =                  # KEY=value
# EnvironmentFile =              # /path/to/file
# EnvironmentHost =              # true | false
# Exec =                         # /bin/sh -c "sleep infinity"
# ExposeHostPort =               # 8080
# GIDMap =                       # 0:100000:65536
# GlobalArgs =                   # --log-level=debug
# Group =                        # string | int
# GroupAdd =                     # wheel | keep-groups
# HealthCmd =                    # /bin/check-health
# HealthInterval =               # 30s
# HealthLogDestination =         # local | /path/to/dir | events_logger
# HealthMaxLogCount =            # 5
# HealthMaxLogSize =             # 500
# HealthOnFailure =              # kill | restart | stop | none
# HealthRetries =                # 3
# HealthStartPeriod =            # 0s
# HealthStartupCmd =             # /bin/startup-check
# HealthStartupInterval =        # 30s
# HealthStartupRetries =         # 0
# HealthStartupSuccess =         # 0
# HealthStartupTimeout =         # 30s
# HealthTimeout =                # 30s
# HostName =                     # string
# HttpProxy =                    # true | false
# Image =                        # docker.io/library/alpine:latest
# ImageVolume =                  # bind | tmpfs | ignore
# IP =                           # 10.88.0.10
# IP6 =                          # fd46:db93:aa76:ac37::10
# Label =                        # key=value
# LogDriver =                    # journald | k8s-file | none | passthrough
# LogOpt =                       # path=/var/log/container.log
# Mask =                         # /proc/acpi:/proc/kcore
# Memory =                       # 512m
# Mount =                        # type=bind,src=/host,dst=/container
# Network =                      # bridge | host | none | <name>
# NetworkAlias =                 # alias
# NoNewPrivileges =              # true | false
# Notify =                       # true | false | healthy | container
# PidsLimit =                    # 2048
# Pod =                          # pod-name
# PodmanArgs =                   # --userns=keep-id
# PublishPort =                  # 8080:80
# Pull =                         # always | missing | never | newer
# ReadOnly =                     # true | false
# ReadOnlyTmpfs =                # true | false
# ReloadCmd =                    # /bin/reload
# ReloadSignal =                 # SIGHUP
# Retry =                        # 3
# RetryDelay =                   # 2s
# Rootfs =                       # /path/to/rootfs:O
# RunInit =                      # true | false
# SeccompProfile =               # /path/to/profile.json
# Secret =                       # mysecret,target=/run/secrets/mysecret
# SecurityLabelDisable =         # true | false
# SecurityLabelFileType =        # container_file_t
# SecurityLabelLevel =           # s0:c100,c200
# SecurityLabelNested =          # true | false
# SecurityLabelType =            # spc_t
# ServiceName =                  # string
# ShmSize =                      # 64m
# StartWithPod =                 # true | false
# StopSignal =                   # SIGTERM
# StopTimeout =                  # 10
# SubGIDMap =                    # @user
# SubUIDMap =                    # @user
# Sysctl =                       # net.ipv4.ip_unprivileged_port_start=80
# Timezone =                     # local | UTC | Europe/Amsterdam
# Tmpfs =                        # /tmp
# UIDMap =                       # 0:100000:65536
# Ulimit =                       # nofile=1024:1024
# Unmask =                       # ALL | /proc/acpi:/proc/kcore
# User =                         # string | int
# UserNS =                       # keep-id | auto | host | ns:/path
# Volume =                       # /host:/container:ro
# WorkingDir =                   # /app


# ---------------------------------------------------------------------------
# podman run OPTIONS (podman-run(1))
# ---------------------------------------------------------------------------
# --add-host =                   # hostname[;hostname[;...]]:ip
# --annotation =                 # key=value
# --arch =                       # ARCH
# --attach =                     # stdin | stdout | stderr
# --authfile =                   # path
# --blkio-weight =               # weight (10-1000)
# --blkio-weight-device =        # device:weight
# --cap-add =                    # capability
# --cap-drop =                   # capability | all
# --cert-dir =                   # path
# --cgroup-conf =                # KEY=VALUE
# --cgroup-parent =              # path
# --cgroupns =                   # host | container:id | private | ns:path
# --cgroups =                    # enabled | disabled | no-conmon | split
# --chrootdirs =                 # path
# --cidfile =                    # file
# --conmon-pidfile =             # file
# --cpu-period =                 # limit (microseconds)
# --cpu-quota =                  # limit (microseconds)
# --cpu-rt-period =              # microseconds
# --cpu-rt-runtime =             # microseconds
# --cpu-shares =                 # shares (default 1024)
# --cpus =                       # number (e.g. 0.5)
# --cpuset-cpus =                # 0,1 | 0-3 | 0-3,7
# --cpuset-mems =                # nodes (e.g. 0,1)
# --creds =                      # [username[:password]]
# --decryption-key =             # key[:passphrase]
# --detach =                     # true | false
# --detach-keys =                # sequence (e.g. ctrl-p,ctrl-q)
# --device =                     # host-device[:container-device][:permissions]
# --device-cgroup-rule =         # "type major:minor mode"
# --device-read-bps =            # path:rate
# --device-read-iops =           # path:rate
# --device-write-bps =           # path:rate
# --device-write-iops =          # path:rate
# --disable-content-trust =      # (NOOP, Docker compat)
# --dns =                        # ipaddr | none
# --dns-option =                 # option
# --dns-search =                 # domain
# --entrypoint =                 # "command" | '["command","arg1"]'
# --env =                        # env | KEY=value
# --env-file =                   # file
# --env-host =                   # (flag)
# --env-merge =                  # env
# --expose =                     # port[/protocol]
# --gidmap =                     # [flags]container_gid:from_gid[:amount]
# --gpus =                       # all | vendor-specific-id
# --group-add =                  # group | keep-groups
# --group-entry =                # ENTRY
# --health-cmd =                 # "command" | '["command","arg1"]'
# --health-interval =            # interval | disable
# --health-log-destination =     # directory_path | local | events_logger
# --health-max-log-count =       # number
# --health-max-log-size =        # size
# --health-on-failure =          # none | kill | restart | stop
# --health-retries =             # retries
# --health-start-period =        # period
# --health-startup-cmd =         # "command" | '["command","arg1"]'
# --health-startup-interval =    # interval | disable
# --health-startup-retries =     # retries
# --health-startup-success =     # retries
# --health-startup-timeout =     # timeout
# --health-timeout =             # timeout
# --hostname =                   # name
# --hosts-file =                 # path | none | image
# --hostuser =                   # name
# --http-proxy =                 # true | false
# --image-volume =               # bind | tmpfs | ignore
# --init =                       # (flag)
# --init-path =                  # path
# --interactive =                # true | false
# --ip =                         # IPv4
# --ip6 =                        # IPv6
# --ipc =                        # "" | container:id | host | none | ns:path | private | shareable
# --label =                      # key=value
# --label-file =                 # file
# --log-driver =                 # k8s-file | journald | none | passthrough | passthrough-tty
# --log-opt =                    # name=value
# --mac-address =                # address
# --memory =                     # number[unit]
# --memory-reservation =         # number[unit]
# --memory-swap =                # number[unit]
# --memory-swappiness =          # number (0-100)
# --mount =                      # type=TYPE,TYPE-SPECIFIC-OPTION[,...]
# --name =                       # name
# --network =                    # mode | bridge[:OPTIONS] | none | container:id | host | ns:path | private | pasta[:OPTIONS]
# --network-alias =              # alias
# --no-healthcheck =             # (flag)
# --no-hostname =                # (flag)
# --no-hosts =                   # (flag)
# --oom-kill-disable =           # (flag)
# --oom-score-adj =              # num (-1000 to 1000)
# --os =                         # OS
# --passwd =                     # (flag)
# --passwd-entry =               # ENTRY
# --personality =                # persona
# --pid =                        # container:id | host | ns:path | private
# --pidfile =                    # path
# --pids-limit =                 # limit
# --platform =                   # OS/ARCH
# --pod =                        # name | new:name
# --pod-id-file =                # file
# --preserve-fd =                # FD1[,FD2,...]
# --preserve-fds =               # N
# --privileged =                 # (flag)
# --publish =                    # [[ip:][hostPort]:]containerPort[/protocol]
# --publish-all =                # (flag)
# --pull =                       # always | missing | never | newer
# --quiet =                      # (flag)
# --rdt-class =                  # intel-rdt-class-of-service
# --read-only =                  # (flag)
# --read-only-tmpfs =            # (flag)
# --replace =                    # (flag)
# --requires =                   # container[,container]
# --restart =                    # no | never | on-failure[:max_retries] | always | unless-stopped
# --retry =                      # attempts
# --retry-delay =                # duration
# --rm =                         # (flag)
# --rmi =                        # (flag)
# --rootfs =                     # exploded rootfs path[:O|:idmap]
# --sdnotify =                   # container | conmon | healthy | ignore
# --seccomp-policy =             # default | image
# --secret =                     # secret[,opt=opt ...]
# --security-opt =               # apparmor=profile | label=disable | mask=/path | no-new-privileges | seccomp=profile.json | unmask=ALL
# --shm-size =                   # number[unit]
# --shm-size-systemd =           # number[unit]
# --sig-proxy =                  # true | false
# --stop-signal =                # signal
# --stop-timeout =               # seconds
# --subgidname =                 # name
# --subuidname =                 # name
# --sysctl =                     # name=value
# --systemd =                    # true | false | always
# --timeout =                    # seconds
# --tls-verify =                 # true | false
# --tmpfs =                      # fs
# --tty =                        # (flag)
# --tz =                         # timezone
# --uidmap =                     # [flags]container_uid:from_uid[:amount]
# --ulimit =                     # option
# --umask =                      # umask
# --unsetenv =                   # env
# --unsetenv-all =               # (flag)
# --user =                       # user[:group]
# --userns =                     # auto[:OPTIONS] | container:id | host | keep-id[:OPTIONS] | nomap | ns:namespace
# --uts =                        # host | private | ns:[path] | container:[container]
# --variant =                    # VARIANT
# --volume =                     # [[SOURCE-VOLUME|HOST-DIR:]CONTAINER-DIR[:OPTIONS]]
# --volumes-from =               # CONTAINER[:OPTIONS]
# --workdir =                    # dir


# ---------------------------------------------------------------------------
# Quadlet [Quadlet] section
# ---------------------------------------------------------------------------
# DefaultDependencies =          # true | false  (disable auto network-online deps)


# ---------------------------------------------------------------------------
# Quadlet [Network] options (podman-network.unit(5))
# ---------------------------------------------------------------------------
# ContainersConfModule =         # /etc/nvd.conf
# DisableDNS =                   # true | false
# DNS =                          # 192.168.55.1
# Driver =                       # bridge | macvlan | ipvlan
# Gateway =                      # 192.168.55.3
# GlobalArgs =                   # --log-level=debug
# InterfaceName =                # enp1s0
# Internal =                     # true | false
# IPAMDriver =                   # dhcp | host-local | none
# IPRange =                      # 192.168.55.128/25 | 10.0.0.10-10.0.0.20
# IPv6 =                         # true | false
# Label =                        # key=value
# NetworkDeleteOnStop =          # true | false
# NetworkName =                  # foo
# Options =                      # mtu=1500 | vlan=10 | isolate=true
# PodmanArgs =                   # --dns=192.168.55.1
# Subnet =                       # 192.5.0.0/16


# ---------------------------------------------------------------------------
# Quadlet [Volume] options (podman-volume.unit(5))
# ---------------------------------------------------------------------------
# ContainersConfModule =         # /etc/nvd.conf
# Copy =                         # true | false
# Device =                       # tmpfs | /dev/sdb1
# Driver =                       # local | image
# GlobalArgs =                   # --log-level=debug
# Group =                        # 192 | groupname
# Image =                        # quay.io/centos/centos:latest
# Label =                        # foo=bar
# Options =                      # nodev,nosuid,noexec | size=512m
# PodmanArgs =                   # --driver=image
# Type =                         # tmpfs | ext4 | xfs
# User =                         # 123 | username
# VolumeName =                   # foo


# ---------------------------------------------------------------------------
# Quadlet [Pod] options (podman-pod.unit(5))
# ---------------------------------------------------------------------------
# AddHost =                      # hostname:ip
# ContainersConfModule =         # /etc/nvd.conf
# DNS =                          # 192.168.55.1
# DNSOption =                    # ndots:1
# DNSSearch =                    # example.com
# ExitPolicy =                   # stop | continue
# GIDMap =                       # 0:10000:10
# GlobalArgs =                   # --log-level=debug
# HostName =                     # name
# IP =                           # 192.5.0.1
# IP6 =                          # 2001:db8::1
# Label =                        # key=value
# Network =                      # host | bridge | none | <name>.network
# NetworkAlias =                 # alias
# PodmanArgs =                   # --cpus=2
# PodName =                      # name
# PublishPort =                  # [[ip:][hostPort]:]containerPort[/protocol]
# ServiceName =                  # name
# ShmSize =                      # 100m
# SubGIDMap =                    # gtest
# SubUIDMap =                    # utest
# UIDMap =                       # 0:10000:10
# UserNS =                       # keep-id:uid=200,gid=210 | auto | host | nomap
# Volume =                       # /source:/dest[:options]


# ---------------------------------------------------------------------------
# Quadlet [Kube] options (podman-kube.unit(5))
# ---------------------------------------------------------------------------
# AutoUpdate =                   # registry | local | name/(local|registry)
# ConfigMap =                    # /tmp/config.map
# ContainersConfModule =         # /etc/nvd.conf
# ExitCodePropagation =          # all | any | none
# GlobalArgs =                   # --log-level=debug
# KubeDownForce =                # true | false
# LogDriver =                    # journald | k8s-file | none | passthrough
# Network =                      # host | bridge | none | <name>.network
# PodmanArgs =                   # --annotation=key=value
# PublishPort =                  # [[ip:][hostPort]:]containerPort[/protocol]
# SetWorkingDirectory =          # yaml | unit
# UserNS =                       # keep-id:uid=200,gid=210 | auto | host | nomap
# Yaml =                         # /path/to/deployment.yml


# ---------------------------------------------------------------------------
# Quadlet [Image] options (podman-image.unit(5))
# ---------------------------------------------------------------------------
# AllTags =                      # true | false
# Arch =                         # aarch64 | arm | amd64
# AuthFile =                     # /etc/registry/auth.json
# CertDir =                      # /etc/registry/certs
# ContainersConfModule =         # /etc/nvd.conf
# Creds =                        # username[:password]
# DecryptionKey =                # /etc/registry.key[:passphrase]
# GlobalArgs =                   # --log-level=debug
# Image =                        # quay.io/centos/centos:latest
# ImageTag =                     # quay.io/centos/centos:latest
# OS =                           # linux | windows | darwin
# PodmanArgs =                   # --os=linux
# Policy =                       # always | missing | never | newer
# Retry =                        # 3
# RetryDelay =                   # 10s
# TLSVerify =                    # true | false
# Variant =                      # arm/v7 | arm/v8


# ---------------------------------------------------------------------------
# Quadlet [Build] options (podman-build.unit(5))
# ---------------------------------------------------------------------------
# Annotation =                   # annotation=value
# Arch =                         # aarch64 | arm | amd64
# AuthFile =                     # /etc/registry/auth.json
# ContainersConfModule =         # /etc/nvd.conf
# DNS =                          # 192.168.55.1
# DNSOption =                    # ndots:1
# DNSSearch =                    # example.com
# Environment =                  # foo=bar
# File =                         # /path/to/Containerfile
# ForceRM =                      # true | false
# GlobalArgs =                   # --log-level=debug
# GroupAdd =                     # wheel | keep-groups
# ImageTag =                     # localhost/imagename
# Label =                        # key=value
# Network =                      # host | none | private | <name>.network
# PodmanArgs =                   # --pull never
# Pull =                         # always | missing | never | newer
# Retry =                        # 3
# RetryDelay =                   # 10s
# Secret =                       # id=mysecret,src=/path | id=mysecret,env=MY_ENV
# SetWorkingDirectory =          # file | unit | /path | https://github.com/repo
# Target =                       # stageName
# TLSVerify =                    # true | false
# Variant =                      # arm/v7
# Volume =                       # /source:/dest[:options]


# ---------------------------------------------------------------------------
# podman --rootfs options
# ---------------------------------------------------------------------------
# --rootfs /path/to/rootfs       # exploded rootfs instead of an image
# --rootfs /path/to/rootfs:O     # overlay (changes discarded on stop)
#                                # :O,upperdir=/some/upper,workdir=/some/work
# -v /host:/container:idmap      # idmapped mount into container user namespace
# -v /host:/container:idmap=uids=0-1-10#10-11-10;gids=0-100-10
# Rootfs =                       # /var/lib/rootfs
# Rootfs =                       # /var/lib/rootfs:O


# ---------------------------------------------------------------------------
# NixOS - virtualisation.podman (nixpkgs module)
# ---------------------------------------------------------------------------
# virtualisation.podman.enable =                         # true | false
# virtualisation.podman.dockerCompat =                   # true | false
# virtualisation.podman.enableNvidia =                   # true | false  (DEPRECATED)
# virtualisation.podman.package =                        # pkgs.podman
# virtualisation.podman.extraPackages =                  # [ pkgs.fuse-overlayfs ]
# virtualisation.podman.extraRuntimes =                  # [ pkgs.crun ]
# virtualisation.podman.autoPrune.enable =               # true | false
# virtualisation.podman.autoPrune.dates =                # "weekly"
# virtualisation.podman.autoPrune.flags =                # [ "--all" ]
# virtualisation.podman.defaultNetwork.settings =        # { dns_enabled = true; }
# virtualisation.podman.dockerSocket.enable =            # true | false
# virtualisation.podman.networkSocket.enable =           # true | false


# ---------------------------------------------------------------------------
# NixOS - virtualisation.oci-containers (nixpkgs module)
# ---------------------------------------------------------------------------
# virtualisation.oci-containers.backend =                # "podman" | "docker"
# virtualisation.oci-containers.containers.<name>.image =             # "docker.io/library/nginx:latest"
# virtualisation.oci-containers.containers.<name>.imageFile =         # pkgs.dockerTools.pullImage { ... }
# virtualisation.oci-containers.containers.<name>.imageStream =       # pkgs.writeShellScript "s" "..."
# virtualisation.oci-containers.containers.<name>.cmd =               # [ "arg1" "arg2" ]
# virtualisation.oci-containers.containers.<name>.entrypoint =        # "/bin/sh"
# virtualisation.oci-containers.containers.<name>.environment =       # { MY_VAR = "value"; }
# virtualisation.oci-containers.containers.<name>.environmentFiles =  # [ /run/secrets/env-file ]
# virtualisation.oci-containers.containers.<name>.user =              # "1000:1000"
# virtualisation.oci-containers.containers.<name>.workdir =           # "/app"
# virtualisation.oci-containers.containers.<name>.hostname =          # "mycontainer"
# virtualisation.oci-containers.containers.<name>.autoStart =         # true | false
# virtualisation.oci-containers.containers.<name>.autoRemove =        # true | false
# virtualisation.oci-containers.containers.<name>.privileged =        # true | false
# virtualisation.oci-containers.containers.<name>.pullPolicy =        # "always" | "missing" | "never" | "newer"
# virtualisation.oci-containers.containers.<name>.volumes =           # [ "/host:/container:ro" ]
# virtualisation.oci-containers.containers.<name>.ports =             # [ "8080:80" ]
# virtualisation.oci-containers.containers.<name>.networks =          # [ "mynet" ]
# virtualisation.oci-containers.containers.<name>.dependsOn =         # [ "other-container" ]
# virtualisation.oci-containers.containers.<name>.labels =            # { "traefik.enable" = "true"; }
# virtualisation.oci-containers.containers.<name>.capabilities =      # { NET_ADMIN = true; }
# virtualisation.oci-containers.containers.<name>.devices =           # [ "/dev/fuse:/dev/fuse" ]
# virtualisation.oci-containers.containers.<name>.log-driver =        # "journald" | "k8s-file" | "none"
# virtualisation.oci-containers.containers.<name>.extraOptions =      # [ "--userns=keep-id" ]
# virtualisation.oci-containers.containers.<name>.extraDockerOptions = # [ "--arg" ]
# virtualisation.oci-containers.containers.<name>.serviceName =       # "myservice"
# virtualisation.oci-containers.containers.<name>.podman.sdnotify =   # "container" | "conmon" | "healthy" | "ignore"
