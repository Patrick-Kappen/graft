version = 1
name = "pac-nixos-managed-example"

[deploy]
enable = true
target = "system"

[config.runtime]
mode = "rootfs-store"
packages = [
  "bashInteractive",
  "coreutils",
]
command = ["bash", "-lc", "echo hello from NixOS-managed pac container"]
