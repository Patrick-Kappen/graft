version = 1
name = "pac-demo"

[parents]
add = [
  "base/runtime",
  "base/locked",
]

[children]
add = ["addons/hostname"]

[deploy]
enable = true
target = "system"

[config.container.environment]
PAC_DEMO = "1"
