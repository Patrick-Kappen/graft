version = 1
name = "nix-check-startup-job-user"

[deploy]
target = "user"
activation = "startup"

[config.runtime]
packages = ["coreutils"]
command = ["/bin/true"]

[config.service]
lifecycle = "job"
