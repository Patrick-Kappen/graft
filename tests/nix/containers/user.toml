version = 1
name = "nix-check-user"

[deploy]
target = "user"

[config.container]
hostname = "nix-check-user.local"
user = "1000"
workingDir = "/workspace"

[config.container.environment]
LOG_LEVEL = "debug"
EMPTY = ""
