version = 1
name = "nix-check-startup-job-system"

[deploy]
target = "system"
activation = "startup"

[config.runtime]
packages = ["coreutils"]
command = ["/bin/true"]

[config.service]
lifecycle = "job"
