version = 1
name = "graft-system-ignored-by-hm"

[deploy]
enable = true
target = "system"

[config.runtime]
mode = "rootfs-store"
packages = ["bashInteractive"]
command = ["bash", "-lc", "echo system"]
