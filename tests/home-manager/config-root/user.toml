version = 1
name = "graft-user-demo"

[parents]
add = ["base"]

[deploy]
enable = true
target = "user"

[config.runtime]
command = ["bash", "-lc", "echo from user quadlet"]

[config.container.environment]
GRAFT_USER_DEMO = "1"
