version = 1
name = "base/runtime"

[config.runtime]
mode = "rootfs-store"
packages = ["bashInteractive"]
command = ["bash", "-lc", "echo from parent"]

[config.container.environment]
FROM_PARENT = "1"
