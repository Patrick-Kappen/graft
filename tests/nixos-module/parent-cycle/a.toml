version = 1
name = "graft-test-cycle-a"

[parents]
add = ["b"]

[deploy]
enable = true
target = "system"

[config.runtime]
mode = "rootfs-store"
packages = ["bashInteractive"]
command = ["bash", "-lc", "echo a"]
