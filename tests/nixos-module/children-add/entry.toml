version = 1
name = "pac-test-children-add"

[children]
add = ["addon"]

[deploy]
enable = true
target = "system"

[config.runtime]
mode = "rootfs-store"
packages = ["bashInteractive"]
command = ["bash", "-lc", "echo from entry"]

[config.container.environment]
FROM_ENTRY = "1"
