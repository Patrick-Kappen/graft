version = 1
name = "pac-test-unknown-package"

[deploy]
enable = true
target = "system"

[config.runtime]
mode = "rootfs-store"
packages = ["definitely-not-a-real-nixpkgs-package-for-pac"]
command = ["bash", "-lc", "echo bad"]
